{# 
name: APP-story-mobile_team
project: APP
issue_type: story
team: mobile-team
description: Mobile-specific user story template with device considerations and cross-platform requirements
defaults: Normal priority, soft commitment, mobile-specific checklist
#}
{
  "fields": {
    "project": {
      "id": "{{ project.id }}"
    },
    "summary": "📱 {{ issue.title }}",
    "description": "{{ issue.description | default('**Mobile User Story**\n\n**As a** [type of mobile user]\n**I want** [mobile functionality]\n**So that** [mobile value/benefit]\n\n**Mobile-Specific Acceptance Criteria**\n\n- [ ] Given [mobile context], when [touch/gesture], then [mobile outcome]\n- [ ] Given [mobile context], when [mobile action], then [mobile outcome]\n- [ ] Given [mobile context], when [mobile interaction], then [mobile outcome]\n\n**Mobile Platform Requirements**\n\n- [ ] iOS compatibility (latest 2 versions)\n- [ ] Android compatibility (latest 2 versions)  \n- [ ] Tablet optimization\n- [ ] Offline functionality considerations\n- [ ] Mobile performance requirements\n\n**Device Considerations**\n\n- [ ] Screen size adaptations (phone/tablet)\n- [ ] Touch interaction patterns\n- [ ] Mobile gestures support\n- [ ] Battery usage optimization\n- [ ] Network connectivity handling\n\n**Mobile Testing Checklist**\n\n- [ ] iOS device testing\n- [ ] Android device testing\n- [ ] Different screen sizes\n- [ ] Portrait/landscape modes\n- [ ] Touch accessibility\n- [ ] Mobile performance testing\n\n**Definition of Done (Mobile)**\n\n- [ ] All acceptance criteria met\n- [ ] Cross-platform compatibility verified\n- [ ] Mobile UX/UI review completed\n- [ ] Performance benchmarks met\n- [ ] Accessibility standards met (mobile)') }}",
    "issuetype": {
      "id": "{{ issue_types.story }}"
    },
    "priority": {
      "id": "{{ priorities[issue.priority] | default(priorities['4 - Normal']) }}"
    },
    "customfield_10129": [
      {
        "id": "{{ team.id | default('12980') }}"
      }
    ],
    "customfield_10119": ["{{ issue.product_backlog | default('Mobile-Stories-Backlog') }}"],
    "customfield_10450": {
      "id": "{{ commitment_levels[issue.commitment_level] | default(commitment_levels['Soft Commitment']) }}"
    },
    "customfield_10167": {
      "id": "{{ areas[issue.area] | default(areas['Features & Innovation']) }}"
    },
    "customfield_10508": {
      "id": "{{ commitment_reasons[issue.commitment_reason] | default(commitment_reasons['Customer Commitment']) }}"
    }
    {%- if issue.parent %},
    "parent": {
      "key": "{{ issue.parent }}"
    }
    {%- endif %}
    {%- if issue.product_priority %},
    "customfield_10327": {
      "id": "{{ product_priorities[issue.product_priority] }}"
    }
    {%- endif %}
    {%- if user.account_id %},
    "customfield_10044": {
      "accountId": "{{ user.account_id }}"
    }
    {%- endif %}
  }
}
